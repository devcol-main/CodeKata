| Solved Date: 2026-07-13-Mon | [Problem Link](https://school.programmers.co.kr/learn/courses/30/lessons/76502?language=cpp)

###### 문제 설명

다음 규칙을 지키는 문자열을 올바른 괄호 문자열이라고 정의합니다.

- `()`, `[]`, `{}` 는 모두 올바른 괄호 문자열입니다.
- 만약 `A`가 올바른 괄호 문자열이라면, `(A)`, `[A]`, `{A}` 도 올바른 괄호 문자열입니다. 예를 들어, `[]` 가 올바른 괄호 문자열이므로, `([])` 도 올바른 괄호 문자열입니다.
- 만약 `A`, `B`가 올바른 괄호 문자열이라면, `AB` 도 올바른 괄호 문자열입니다. 예를 들어, `{}` 와 `([])` 가 올바른 괄호 문자열이므로, `{}([])` 도 올바른 괄호 문자열입니다.

대괄호, 중괄호, 그리고 소괄호로 이루어진 문자열 `s`가 매개변수로 주어집니다. 이 `s`를 왼쪽으로 x (_0 ≤ x < (`s`의 길이)_) 칸만큼 회전시켰을 때 `s`가 올바른 괄호 문자열이 되게 하는 x의 개수를 return 하도록 solution 함수를 완성해주세요.

---

##### 제한사항

- s의 길이는 1 이상 1,000 이하입니다.

---

##### 입출력 예

|s|result|
|---|---|
|`"[](){}"`|3|
|`"}]()[{"`|2|
|`"[)(]"`|0|
|`"}}}"`|0|

---

##### 입출력 예 설명

**입출력 예 #1**

- 다음 표는 `"[](){}"` 를 회전시킨 모습을 나타낸 것입니다.

|x|s를 왼쪽으로 x칸만큼 회전|올바른 괄호 문자열?|
|---|---|---|
|0|`"[](){}"`|O|
|1|`"](){}["`|X|
|2|`"(){}[]"`|O|
|3|`"){}[]("`|X|
|4|`"{}[]()"`|O|
|5|`"}[](){"`|X|

- 올바른 괄호 문자열이 되는 x가 3개이므로, 3을 return 해야 합니다.

**입출력 예 #2**

- 다음 표는 `"}]()[{"` 를 회전시킨 모습을 나타낸 것입니다.

|x|s를 왼쪽으로 x칸만큼 회전|올바른 괄호 문자열?|
|---|---|---|
|0|`"}]()[{"`|X|
|1|`"]()[{}"`|X|
|2|`"()[{}]"`|O|
|3|`")[{}]("`|X|
|4|`"[{}]()"`|O|
|5|`"{}]()["`|X|

- 올바른 괄호 문자열이 되는 x가 2개이므로, 2를 return 해야 합니다.

**입출력 예 #3**

- s를 어떻게 회전하더라도 올바른 괄호 문자열을 만들 수 없으므로, 0을 return 해야 합니다.

**입출력 예 #4**

- s를 어떻게 회전하더라도 올바른 괄호 문자열을 만들 수 없으므로, 0을 return 해야 합니다.

---
### 기본 코드
```cpp
#include <string>
#include <vector>

using namespace std;

int solution(string s) {
    int answer = -1;
    return answer;
}
```

---
### 힌트

정답은 **문자열의 길이만큼 반복하며 왼쪽으로 한 칸씩 회전(Rotation)시킨 후, 자료구조 스택(Stack)을 활용하여 올바른 괄호 문자열인지 검사하는 것**입니다.

---
## 문제 풀이 과정


1. **문자열 회전 구현하기**
    - 문자열을 왼쪽으로 \(1\)칸 회전하는 것은 가장 첫 번째 글자를 맨 뒤로 보내는 것과 같습니다.
    - C++에서는 `s.substr(1) + s[0]` 형태를 사용하거나 `std::rotate` 함수를 사용하면 쉽게 구현할 수 있습니다.
2. **올바른 괄호 검사 (스택 활용)**
    - 열린 괄호(`(`, `[`, `{`)가 나오면 스택에 `push`합니다.
    - 닫힌 괄호(`)`, `]`, `}`)가 나오면 스택의 맨 위(`top`)에 있는 글자가 짝이 맞는 열린 괄호인지 확인합니다.
    - 짝이 맞으면 스택에서 `pop`하고, 짝이 맞지 않거나 스택이 비어있다면 올바르지 않은 괄호 문자열입니다.
    - 문자열을 끝까지 검사했을 때 스택이 완전히 비어 있어야 올바른 괄호 문자열입니다. 


---
## 정답 소스 코드
```cpp
#include <string>
#include <vector>
#include <stack>

using namespace std;

// 올바른 괄호 문자열인지 검사하는 함수
bool isValid(string str) {
    stack<char> s;
    
    for (char c : str) {
        // 열린 괄호는 스택에 삽입
        if (c == '(' || c == '[' || c == '{') {
            s.push(c);
        } 
        // 닫힌 괄호 처리
        else {
            if (s.empty()) return false; // 짝을 맞출 열린 괄호가 없음
            
            char top = s.top();
            if ((c == ')' && top == '(') ||
                (c == ']' && top == '[') ||
                (c == '}' && top == '{')) {
                s.pop(); // 짝이 맞으므로 제거
            } else {
                return false; // 짝이 맞지 않음
            }
        }
    }
    
    return s.empty(); // 최종적으로 스택이 비어있어야 올바른 괄호
}

int solution(string s) 
{
    int answer = 0;
    int len = s.length();
    
    // 문자열 길이만큼 회전시키며 검사
    for (int i = 0; i < len; i++) {
        if (isValid(s)) {
            answer++;
        }
        // 왼쪽으로 1칸 회전 (첫 글자를 맨 뒤로 보냄)
        s = s.substr(1) + s[0];
    }
    
    return answer;
}


```


---

## Comments:

* 위 로직을 사용하면 모든 회전 케이스(\(x\))에 대해 시간 복잡도 \(O(N^2)\) 내로 효율적으로 올바른 괄호 문자열의 개수를 구할 수 있습니다.