| Solved Date: 2026-06-25-Thur | [Problem Link](https://school.programmers.co.kr/learn/courses/30/lessons/12939?language=cpp)



###### 문제 설명

문자열 s에는 공백으로 구분된 숫자들이 저장되어 있습니다. str에 나타나는 숫자 중 최소값과 최대값을 찾아 이를 "(최소값) (최대값)"형태의 문자열을 반환하는 함수, solution을 완성하세요.  
예를들어 s가 "1 2 3 4"라면 "1 4"를 리턴하고, "-1 -2 -3 -4"라면 "-4 -1"을 리턴하면 됩니다.

##### 제한 조건

- s에는 둘 이상의 정수가 공백으로 구분되어 있습니다.

##### 입출력 예

|s|return|
|---|---|
|"1 2 3 4"|"1 4"|
|"-1 -2 -3 -4"|"-4 -1"|
|"-1 -1"|"-1 -1"|

---
### 기본 코드
```cpp
#include <string>
#include <vector>

using namespace std;

string solution(string s) {
    string answer = "";
    return answer;
}
```

---
### 힌트
* stringstream
* minmax_element
* to_string


---
## 정답 소스 코드
```cpp
#include <string>
#include <vector>
#include <sstream>
#include <algorithm>

using namespace std;

string solution(string s) 
{
    stringstream ss(s);
    int num;
    vector<int> numbers;
    
    // 1. 공백을 기준으로 숫자를 파싱하여 벡터에 저장
    while (ss >> num) 
    {
        numbers.push_back(num);
    }
    
    // 2. 벡터에서 최솟값과 최댓값의 위치(반복자)를 찾음
    auto [min_it, max_it] = minmax_element(numbers.begin(), numbers.end());
    
    // 3. "(최솟값) (최댓값)" 형태의 문자열로 변환하여 반환
    return to_string(*min_it) + " " + to_string(*max_it);
}

```


